apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: db-secrets-provider
spec:
  provider: gcp
  parameters:
    secrets: |
      - resourceName: "projects/YOUR_PROJECT_ID/secrets/YOUR_GCP_APP_NAME-RESOURCE_SUFFIX-db-password/versions/latest"
        fileName: "db-password"
  secretObjects: # 自動將 Secret Manager 的值同步成 K8s Secret
  - secretName: db-password-secret  # 這將會是產生的 K8s Secret 名稱
    type: Opaque
    data:
    - objectName: "db-password"     # 必須與上面 fileName 的名稱一致！
      key: password                 # 產生的 K8s Secret 內的 Key