name: Build and Deploy to GKE

on:
  push:
    branches: [ "main", "uat", "dev" ] # 監聽三個分支
  workflow_dispatch: # 允許手動觸發

env:
  GAR_LOCATION: asia-east1 
  REPOSITORY: test-k8s-app-repo
  IMAGE: my-app
  CLUSTER_NAME: test-k8s-app-cluster
  CLUSTER_LOCATION: asia-east1

jobs:
  setup-build-publish-deploy:
    name: Setup, Build, Publish, and Deploy
    runs-on: ubuntu-latest
    
    # main -> production
    # uat -> uat
    # dev -> development
    environment: >-
      ${{ github.ref_name == 'main' && 'production' || 
          github.ref_name == 'uat'  && 'uat' || 
          'development' }}
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Checkout
      uses: actions/checkout@v5

    # 1. WIF 認證 (這部分與你原本的 IaC workflow 類似)
    - id: 'auth'
      name: 'Authenticate to Google Cloud'
      uses: 'google-github-actions/auth@v3'
      with:
        token_format: 'access_token'
        workload_identity_provider: '${{ vars.GCP_WIF_PROVIDER }}'
        service_account: '${{ vars.GCP_SERVICE_ACCOUNT }}'
        create_credentials_file: true # 1. 產生憑證檔案，這是執行 override 的前提條件

    # 2. 登入 Artifact Registry (讓 Docker 具備推送到 GCP 的權限)
    - name: Docker Auth
      id: docker-auth
      uses: 'docker/login-action@v3'
      with:
        username: 'oauth2accesstoken'
        password: '${{ steps.auth.outputs.access_token }}'
        registry: '${{ env.GAR_LOCATION }}-docker.pkg.dev'

    # 3. 打包並推送 Docker Image
    # 使用 GitHub Run ID 作為 Tag，確保每次部署的 Image 都是唯一的
    - name: Build and Push Container
      run: |-
        docker build -t "${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE }}:${{ github.sha }}" .
        docker push "${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE }}:${{ github.sha }}"
        
    # 4. 獲取 GKE 憑證 (連結到你的 K8s 工地)
    - name: Set up GKE credentials
      uses: 'google-github-actions/get-gke-credentials@v2'
      with:
        cluster_name: '${{ vars.GKE_CLUSTER_NAME }}'
        location: '${{ env.CLUSTER_LOCATION }}'
        project_id: '${{ vars.GCP_PROJECT_ID }}' # 2. 強制指定目標專案

    # 5. 部署到 GKE
    - name: Deploy to GKE
      run: |-
        # 1. 決定環境標籤 (main -> prod, 其他維持原分支名)
        TARGET_NS=${{ github.ref_name == 'main' && 'prod' || github.ref_name }}
        
        # 2. 決定對應的網域 (請將 kyjhome.com 換成你的實際網域)
        if [ "$TARGET_NS" == "prod" ]; then
          DOMAIN="test-k8s-app.kyjhome.com"
        else
          DOMAIN="test-k8s-app-$TARGET_NS.kyjhome.com"
        fi

        echo "Deploying to Namespace: $TARGET_NS"
        echo "Using Domain: $DOMAIN"
        echo "Using Static IP Name: test-k8s-static-ip-$TARGET_NS"

        # 3. 確保 Namespace 存在
        kubectl create namespace $TARGET_NS --dry-run=client -o yaml | kubectl apply -f -

        # 4. 替換 Deployment 裡的 Image 路徑
        IMAGE_PATH="${{ env.GAR_LOCATION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.IMAGE }}:${{ github.sha }}"
        sed -i "s|image: .*|image: $IMAGE_PATH|g" k8s/deployment.yaml

        # 5. 替換 Ingress 裡的靜態 IP 名稱 (加上 -app 以匹配 Terraform)
        sed -i "s|STATIC_IP_NAME|test-k8s-app-static-ip-$TARGET_NS|g" k8s/ingress.yaml
        
        # 6. 替換 Ingress 裡的網域佔位符
        sed -i "s|DOMAIN_NAME_PLACEHOLDER|$DOMAIN|g" k8s/ingress.yaml
    
        # 7. 部署所有資源到指定的 Namespace
        kubectl apply -f k8s/ -n $TARGET_NS
        